# Project Roadmap

## Текущий этап (2026-05-27)

### Завершённые backend-pass этапы
1. **Backend strict-pass**
- PL/SQL backend завершён.
- Центральный API: `pkg_genetics_game`.
- spec/body компилируются, `user_errors` пустой.
- smoke-tests `01..08` прошли (`Failed: 0`).

2. **Content compliance pass (ЛР2/KB)**
- Расширен seed-контент по признакам, мутациям и заданиям.
- Текущие объёмы: `genes=12`, `alleles=24`, `mutations=8`, `mutation_rules=12`, `tasks=12`, `task_markers=21`.
- Покрытие: универсальные признаки + `species_type 1..6`.

3. **Economy pass**
- `apply_mutation` применяет `mutations.rating_effect` к рейтингу лаборатории.
- `apply_mutagen` получил экономику:
  - `RADIATION`: `cost=50`, `rating_delta=-5`.
  - `CHEMICAL`: `cost=100`, `rating_delta=-2`.
- Рейтинг ограничен снизу через `greatest(0, ...)`.
- Auto-complete задач может компенсировать штрафы.

4. **Multiuser strict-pass**
- Session-bound доступ к лаборатории усилен.
- Одна лаборатория не может быть открыта в двух ACTIVE sessions одновременно.
- Ошибки:
  - `-20072` lab already opened in another active session;
  - `-20073` selected lab is not active in current session.
- Добавлен `08_multiuser_sessions_smoke_test.sql`.

### GUI готовность
Реализованы экраны:
- Auth
- Lab Selection
- Main Shell
- Существа
- Генетический эксперимент
- Мутации
- Задания
- История экспериментов

### Локализация и display-layer
- Используется единый mapping в `python_client/app/services/display_names.py`.
- Пользовательское отображение русифицировано.
- Бизнес-логика остаётся в PL/SQL.

### Закрытый инцидент
- Исправлена сломанная кодировка во вкладке «Задания» (`tasks_tab.py`, `display_names.py`).
- Восстановлены корректные UTF-8 строки и mapping task names.

## Актуальный следующий этап
**Аудит расширения признаков/контента по ЛР1/ЛР2 и KB — выполнен.**

Вывод: текущий seed достаточен для базового строгого соответствия ЛР1/ЛР2, но для более сильного учебного впечатления можно выполнить отдельный точечный content-pass.

Рекомендуемый порядок, если расширение будет подтверждено:
1. Согласовать конкретные признаки/аллели из KB-таблицы, не добавляя контент наугад.
2. Расширить только seed-данные, если достаточно: `genes`, `alleles`, `mutations`, `mutation_rules`, `tasks`, `task_markers`.
3. Обновить `display_names.py` для новых пользовательских подписей.
4. Обновить smoke-tests `02` и `07` под новое покрытие.
5. Прогнать seed, `02`, `07`, затем полный smoke-прогон `01..08`.

## Что уже закрыто после предыдущих этапов
- GUI closeEvent/logout fix завершён: закрытие через `X` больше не должно оставлять лабораторию занятой.
- Dev-only скрипт разблокировки старых зависших sessions добавлен: `database/scripts/dev_unlock_stale_sessions.sql`.
- История экспериментов получает реальный `experiments.created_at`.

## Возможные отдельные треки
1. **Seed-only content enrichment**:
- усилить демонстрацию `linkage_group` там, где группа содержит только один ген;
- добавить задания/мутации только под подтверждённые KB-признаки;
- расширить display mappings и тесты.

2. **Backend/DDL history enrichment**:
- хранить тип мутагена и/или изменённый ген для MUTAGEN history, если это потребуется для защиты/демонстрации.

3. **DDL generation track**:
- добавить поколение существа (`generation`), если нужно строго показывать поколение в коллекции, как в расширенном GUI-требовании ЛР.

4. **Финальный UX polish**:
- dashboard;
- больше карточек вместо перегруженных таблиц;
- комфортный первый запуск для преподавателя.

## Content/GUI pass: формулировки заданий
- Обновлены описания заданий в seed без изменения backend-модели.
- Для задач, выполнимых стартовыми существами, применены формулировки Найдите/Отберите/предъявите вместо Получите.
- В roadmap зафиксировано: явные типы задач (FIND/BREED/MUTATE) — отдельный DDL/backend-трек при отдельном подтверждении.


## Финальный UX polish — Этап 1 закрыт
Сделан первый безопасный проход user-friendly polish (onboarding + пустые состояния) без изменения backend-логики.
Следующий шаг: Этап 2 — разгрузка таблиц и повышение читабельности (без изменения бизнес-логики).

## Обновление: coherence pass
- Разделены смешанные мутации по видам, чтобы apply_mutation не требовал невозможный набор gene_id.
- Добавлены проверки в tests 02/07 на отсутствие mixed species-specific rule sets в одной мутации.

